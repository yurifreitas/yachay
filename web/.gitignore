node_modules/
dist/
# Tracked, not ignored: see the note in the repository-root .gitignore. Without these the
# published site has no numbers, and CI cannot regenerate them from a clone.
# src/data/generated/

# TypeScript incremental build state: a local cache, regenerated by every build.
tsconfig.tsbuildinfo
*.tsbuildinfo
